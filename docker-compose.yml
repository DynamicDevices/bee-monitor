version: '2'
services:
  av:
    build: ./av
    privileged: true
    expose:
      - "80"
    volumes:
      - shared-data:/data

#  sensors:
#    build: ./sensors
#    privileged: true
#    volumes:
#      - shared-data:/data

#  prometheus:
#    build: ./prometheus
#    ports:
#      - "80:9090"
#    volumes:
#      - shared-data:/data

  node_exporter:
    build: ./node_exporter
    ports:
      - "80:9100"
    volumes:
      - shared-data:/data

volumes:
  shared-data:
